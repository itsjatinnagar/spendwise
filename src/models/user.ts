export type User = {
  id: string;
  name: string;
  email: string;
  mobile: string;
  createdAt: Date;
};

export type UserRow = {
  id: string;
  name: string;
  email: string;
  mobile: string;
  created_at: string;
};
