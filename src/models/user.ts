export type User = {
  id: string;
  fullName: string;
  email: string;
  currency: string;
  createdAt: Date;
};

export type UserRow = {
  id: string;
  full_name: string;
  email: string;
  currency: string;
  created_at: string;
};
